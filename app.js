/* =========================================================================
   Bank Przykładowy — Asystent klienta
   Logika aplikacji (vanilla JS, bez frameworków, bez backendu).
   Kolejne funkcje dodawane są w następnych krokach.
   ========================================================================= */
"use strict";

(function () {
  // Punkt wejścia — uruchamiany po załadowaniu DOM.
  function init() {
    // Interfejs zostanie podłączony w kolejnych krokach.
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", init);
  } else {
    init();
  }
})();
