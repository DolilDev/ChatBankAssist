# Dependencies (CI only — the app itself uses no Node.js)
node_modules/
package-lock.json

# Build output (generated by GitHub Actions, not committed)
dist/

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.log

# Local environment / secrets — API keys NEVER belong in the repo
.env
.env.*
!.env.example
*.key
secrets.json
